/* This file was generated by ODB, object-relational mapping (ORM)
 * compiler for C++.
 */

DROP TABLE IF EXISTS "inverse_employee_projects" CASCADE;

DROP TABLE IF EXISTS "inverse_employee" CASCADE;

DROP TABLE IF EXISTS "inverse_project" CASCADE;

DROP TABLE IF EXISTS "inverse_position" CASCADE;

DROP TABLE IF EXISTS "inverse_employer" CASCADE;

CREATE TABLE "inverse_employer" (
  "name" TEXT NOT NULL PRIMARY KEY);

CREATE TABLE "inverse_position" (
  "id" BIGSERIAL NOT NULL PRIMARY KEY,
  "title" TEXT NOT NULL);

CREATE TABLE "inverse_project" (
  "name" TEXT NOT NULL PRIMARY KEY);

CREATE TABLE "inverse_employee" (
  "id" BIGSERIAL NOT NULL PRIMARY KEY,
  "first" TEXT NOT NULL,
  "last" TEXT NOT NULL,
  "employer" TEXT NOT NULL,
  "position" BIGINT NOT NULL,
  CONSTRAINT "employer_fk"
    FOREIGN KEY ("employer")
    REFERENCES "inverse_employer" ("name")
    INITIALLY DEFERRED,
  CONSTRAINT "position_fk"
    FOREIGN KEY ("position")
    REFERENCES "inverse_position" ("id")
    INITIALLY DEFERRED);

CREATE TABLE "inverse_employee_projects" (
  "object_id" BIGINT NOT NULL,
  "value" TEXT NOT NULL,
  CONSTRAINT "object_id_fk"
    FOREIGN KEY ("object_id")
    REFERENCES "inverse_employee" ("id")
    ON DELETE CASCADE,
  CONSTRAINT "value_fk"
    FOREIGN KEY ("value")
    REFERENCES "inverse_project" ("name")
    INITIALLY DEFERRED);

CREATE INDEX "inverse_employee_projects_object_id_i"
  ON "inverse_employee_projects" ("object_id");

