// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include "person-odb.hxx"

#include <cassert>
#include <cstring>  // std::memcpy


#include <odb/pgsql/traits.hxx>
#include <odb/pgsql/database.hxx>
#include <odb/pgsql/transaction.hxx>
#include <odb/pgsql/connection.hxx>
#include <odb/pgsql/statement.hxx>
#include <odb/pgsql/statement-cache.hxx>
#include <odb/pgsql/simple-object-statements.hxx>
#include <odb/pgsql/container-statements.hxx>
#include <odb/pgsql/exceptions.hxx>
#include <odb/pgsql/simple-object-result.hxx>

namespace odb
{
  // basic_name
  //

  bool access::composite_value_traits< ::basic_name, id_pgsql >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // first_
    //
    if (t[0UL])
    {
      i.first_value.capacity (i.first_size);
      grew = true;
    }

    // last_
    //
    if (t[1UL])
    {
      i.last_value.capacity (i.last_size);
      grew = true;
    }

    return grew;
  }

  void access::composite_value_traits< ::basic_name, id_pgsql >::
  bind (pgsql::bind* b,
        image_type& i,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (b);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    std::size_t n (0);
    ODB_POTENTIALLY_UNUSED (n);

    // first_
    //
    b[n].type = pgsql::bind::text;
    b[n].buffer = i.first_value.data ();
    b[n].capacity = i.first_value.capacity ();
    b[n].size = &i.first_size;
    b[n].is_null = &i.first_null;
    n++;

    // last_
    //
    b[n].type = pgsql::bind::text;
    b[n].buffer = i.last_value.data ();
    b[n].capacity = i.last_value.capacity ();
    b[n].size = &i.last_size;
    b[n].is_null = &i.last_null;
    n++;
  }

  bool access::composite_value_traits< ::basic_name, id_pgsql >::
  init (image_type& i,
        const value_type& o,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    bool grew (false);

    // first_
    //
    {
      ::std::string const& v =
        o.first_;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.first_value.capacity ());
      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_image (
        i.first_value,
        size,
        is_null,
        v);
      i.first_null = is_null;
      i.first_size = size;
      grew = grew || (cap != i.first_value.capacity ());
    }

    // last_
    //
    {
      ::std::string const& v =
        o.last_;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.last_value.capacity ());
      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_image (
        i.last_value,
        size,
        is_null,
        v);
      i.last_null = is_null;
      i.last_size = size;
      grew = grew || (cap != i.last_value.capacity ());
    }

    return grew;
  }

  void access::composite_value_traits< ::basic_name, id_pgsql >::
  init (value_type& o,
        const image_type&  i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // first_
    //
    {
      ::std::string& v =
        o.first_;

      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_value (
        v,
        i.first_value,
        i.first_size,
        i.first_null);
    }

    // last_
    //
    {
      ::std::string& v =
        o.last_;

      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_value (
        v,
        i.last_value,
        i.last_size,
        i.last_null);
    }
  }

  // name_extras
  //

  // aliases_
  //

  void access::composite_value_traits< ::name_extras, id_pgsql >::aliases_traits::
  bind (pgsql::bind* b,
        const pgsql::bind* id,
        std::size_t id_size,
        data_image_type& d)
  {
    using namespace pgsql;

    statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    size_t n (0);

    // object_id
    //
    if (id != 0)
      std::memcpy (&b[n], id, id_size * sizeof (id[0]));
    n += id_size;

    // index
    //
    b[n].type = pgsql::bind::bigint;
    b[n].buffer = &d.index_value;
    b[n].is_null = &d.index_null;
    n++;

    // value
    //
    composite_value_traits< value_type, id_pgsql >::bind (
      b + n, d.value_value, sk);
  }

  void access::composite_value_traits< ::name_extras, id_pgsql >::aliases_traits::
  grow (data_image_type& i,
        bool* t)
  {
    bool grew (false);

    // index
    //
    t[0UL] = 0;

    // value
    //
    if (composite_value_traits< value_type, id_pgsql >::grow (
          i.value_value, t + 1UL))
      grew = true;

    if (grew)
      i.version++;
  }

  void access::composite_value_traits< ::name_extras, id_pgsql >::aliases_traits::
  init (data_image_type& i,
        index_type* j,
        const value_type& v)
  {
    using namespace pgsql;

    statement_kind sk (statement_insert);
    ODB_POTENTIALLY_UNUSED (sk);

    bool grew (false);

    // index
    //
    if (j != 0)
    {
      bool is_null (false);
      pgsql::value_traits<
          index_type,
          pgsql::id_bigint >::set_image (
        i.index_value, is_null, *j);
      i.index_null = is_null;
    }

    // value
    //
    {
      if (composite_value_traits< value_type, id_pgsql >::init (
            i.value_value,
            v,
            sk))
        grew = true;
    }

    if (grew)
      i.version++;
  }

  void access::composite_value_traits< ::name_extras, id_pgsql >::aliases_traits::
  init (index_type& j,
        value_type& v,
        const data_image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (db);

    // index
    //
    {
      pgsql::value_traits<
          index_type,
          pgsql::id_bigint >::set_value (
        j,
        i.index_value,
        i.index_null);
    }

    // value
    //
    {
      composite_value_traits< value_type, id_pgsql >::init (
        v,
        i.value_value,
        db);
    }
  }

  void access::composite_value_traits< ::name_extras, id_pgsql >::aliases_traits::
  insert (index_type i, const value_type& v, void* d)
  {
    using namespace pgsql;

    statements_type& sts (*static_cast< statements_type* > (d));
    data_image_type& di (sts.data_image ());

    init (di, &i, v);

    if (sts.data_binding_test_version ())
    {
      const binding& id (sts.id_binding ());
      bind (sts.data_bind (), id.bind, id.count, di);
      sts.data_binding_update_version ();
    }

    if (!sts.insert_statement ().execute ())
      throw object_already_persistent ();
  }

  bool access::composite_value_traits< ::name_extras, id_pgsql >::aliases_traits::
  select (index_type& i, value_type& v, void* d)
  {
    using namespace pgsql;
    using pgsql::select_statement;

    statements_type& sts (*static_cast< statements_type* > (d));
    data_image_type& di (sts.data_image ());

    init (i, v, di, &sts.connection ().database ());

    select_statement& st (sts.select_statement ());
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      grow (di, sts.select_image_truncated ());

      if (sts.data_binding_test_version ())
      {
        bind (sts.data_bind (), 0, sts.id_binding ().count, di);
        sts.data_binding_update_version ();
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  void access::composite_value_traits< ::name_extras, id_pgsql >::aliases_traits::
  delete_ (void* d)
  {
    using namespace pgsql;

    statements_type& sts (*static_cast< statements_type* > (d));
    sts.delete_statement ().execute ();
  }

  void access::composite_value_traits< ::name_extras, id_pgsql >::aliases_traits::
  persist (const container_type& c,
           statements_type& sts)
  {
    using namespace pgsql;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::persist (c, fs);
  }

  void access::composite_value_traits< ::name_extras, id_pgsql >::aliases_traits::
  load (container_type& c,
        statements_type& sts)
  {
    using namespace pgsql;
    using pgsql::select_statement;

    const binding& id (sts.id_binding ());

    if (sts.data_binding_test_version ())
    {
      bind (sts.data_bind (), id.bind, id.count, sts.data_image ());
      sts.data_binding_update_version ();
    }

    select_statement& st (sts.select_statement ());
    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      data_image_type& di (sts.data_image ());
      grow (di, sts.select_image_truncated ());

      if (sts.data_binding_test_version ())
      {
        bind (sts.data_bind (), 0, id.count, di);
        sts.data_binding_update_version ();
        st.refetch ();
      }
    }

    bool more (r != select_statement::no_data);

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::load (c, more, fs);
  }

  void access::composite_value_traits< ::name_extras, id_pgsql >::aliases_traits::
  update (const container_type& c,
          statements_type& sts)
  {
    using namespace pgsql;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::update (c, fs);
  }

  void access::composite_value_traits< ::name_extras, id_pgsql >::aliases_traits::
  erase (statements_type& sts)
  {
    using namespace pgsql;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::erase (fs);
  }

  bool access::composite_value_traits< ::name_extras, id_pgsql >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // nickname_
    //
    if (t[0UL])
    {
      i.nickname_value.capacity (i.nickname_size);
      grew = true;
    }

    return grew;
  }

  void access::composite_value_traits< ::name_extras, id_pgsql >::
  bind (pgsql::bind* b,
        image_type& i,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (b);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    std::size_t n (0);
    ODB_POTENTIALLY_UNUSED (n);

    // nickname_
    //
    b[n].type = pgsql::bind::text;
    b[n].buffer = i.nickname_value.data ();
    b[n].capacity = i.nickname_value.capacity ();
    b[n].size = &i.nickname_size;
    b[n].is_null = &i.nickname_null;
    n++;
  }

  bool access::composite_value_traits< ::name_extras, id_pgsql >::
  init (image_type& i,
        const value_type& o,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    bool grew (false);

    // nickname_
    //
    {
      ::std::string const& v =
        o.nickname_;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.nickname_value.capacity ());
      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_image (
        i.nickname_value,
        size,
        is_null,
        v);
      i.nickname_null = is_null;
      i.nickname_size = size;
      grew = grew || (cap != i.nickname_value.capacity ());
    }

    return grew;
  }

  void access::composite_value_traits< ::name_extras, id_pgsql >::
  init (value_type& o,
        const image_type&  i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // nickname_
    //
    {
      ::std::string& v =
        o.nickname_;

      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_value (
        v,
        i.nickname_value,
        i.nickname_size,
        i.nickname_null);
    }
  }

  // name
  //

  bool access::composite_value_traits< ::name, id_pgsql >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // basic_name base
    //
    if (composite_value_traits< ::basic_name, id_pgsql >::grow (
          i, t + 0UL))
      grew = true;

    // title_
    //
    if (t[2UL])
    {
      i.title_value.capacity (i.title_size);
      grew = true;
    }

    // extras_
    //
    if (composite_value_traits< ::name_extras, id_pgsql >::grow (
          i.extras_value, t + 3UL))
      grew = true;

    return grew;
  }

  void access::composite_value_traits< ::name, id_pgsql >::
  bind (pgsql::bind* b,
        image_type& i,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (b);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    std::size_t n (0);
    ODB_POTENTIALLY_UNUSED (n);

    // basic_name base
    //
    composite_value_traits< ::basic_name, id_pgsql >::bind (b + n, i, sk);
    n += 2UL;

    // title_
    //
    b[n].type = pgsql::bind::text;
    b[n].buffer = i.title_value.data ();
    b[n].capacity = i.title_value.capacity ();
    b[n].size = &i.title_size;
    b[n].is_null = &i.title_null;
    n++;

    // extras_
    //
    composite_value_traits< ::name_extras, id_pgsql >::bind (
      b + n, i.extras_value, sk);
    n += 1UL;
  }

  bool access::composite_value_traits< ::name, id_pgsql >::
  init (image_type& i,
        const value_type& o,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    bool grew (false);

    // basic_name base
    //
    if (composite_value_traits< ::basic_name, id_pgsql >::init (i, o, sk))
      grew = true;

    // title_
    //
    {
      ::std::string const& v =
        o.title_;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.title_value.capacity ());
      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_image (
        i.title_value,
        size,
        is_null,
        v);
      i.title_null = is_null;
      i.title_size = size;
      grew = grew || (cap != i.title_value.capacity ());
    }

    // extras_
    //
    {
      ::name_extras const& v =
        o.extras_;

      if (composite_value_traits< ::name_extras, id_pgsql >::init (
            i.extras_value,
            v,
            sk))
        grew = true;
    }

    return grew;
  }

  void access::composite_value_traits< ::name, id_pgsql >::
  init (value_type& o,
        const image_type&  i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // basic_name base
    //
    composite_value_traits< ::basic_name, id_pgsql >::init (o, i, db);

    // title_
    //
    {
      ::std::string& v =
        o.title_;

      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_value (
        v,
        i.title_value,
        i.title_size,
        i.title_null);
    }

    // extras_
    //
    {
      ::name_extras& v =
        o.extras_;

      composite_value_traits< ::name_extras, id_pgsql >::init (
        v,
        i.extras_value,
        db);
    }
  }

  // phone_numbers
  //

  bool access::composite_value_traits< ::phone_numbers, id_pgsql >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // first
    //
    if (t[0UL])
    {
      i.first_value.capacity (i.first_size);
      grew = true;
    }

    // second
    //
    if (t[1UL])
    {
      i.second_value.capacity (i.second_size);
      grew = true;
    }

    return grew;
  }

  void access::composite_value_traits< ::phone_numbers, id_pgsql >::
  bind (pgsql::bind* b,
        image_type& i,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (b);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    std::size_t n (0);
    ODB_POTENTIALLY_UNUSED (n);

    // first
    //
    b[n].type = pgsql::bind::text;
    b[n].buffer = i.first_value.data ();
    b[n].capacity = i.first_value.capacity ();
    b[n].size = &i.first_size;
    b[n].is_null = &i.first_null;
    n++;

    // second
    //
    b[n].type = pgsql::bind::text;
    b[n].buffer = i.second_value.data ();
    b[n].capacity = i.second_value.capacity ();
    b[n].size = &i.second_size;
    b[n].is_null = &i.second_null;
    n++;
  }

  bool access::composite_value_traits< ::phone_numbers, id_pgsql >::
  init (image_type& i,
        const value_type& o,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    bool grew (false);

    // first
    //
    {
      ::std::basic_string< char > const& v =
        o.first;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.first_value.capacity ());
      pgsql::value_traits<
          ::std::basic_string< char >,
          pgsql::id_string >::set_image (
        i.first_value,
        size,
        is_null,
        v);
      i.first_null = is_null;
      i.first_size = size;
      grew = grew || (cap != i.first_value.capacity ());
    }

    // second
    //
    {
      ::std::basic_string< char > const& v =
        o.second;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.second_value.capacity ());
      pgsql::value_traits<
          ::std::basic_string< char >,
          pgsql::id_string >::set_image (
        i.second_value,
        size,
        is_null,
        v);
      i.second_null = is_null;
      i.second_size = size;
      grew = grew || (cap != i.second_value.capacity ());
    }

    return grew;
  }

  void access::composite_value_traits< ::phone_numbers, id_pgsql >::
  init (value_type& o,
        const image_type&  i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // first
    //
    {
      ::std::basic_string< char >& v =
        o.first;

      pgsql::value_traits<
          ::std::basic_string< char >,
          pgsql::id_string >::set_value (
        v,
        i.first_value,
        i.first_size,
        i.first_null);
    }

    // second
    //
    {
      ::std::basic_string< char >& v =
        o.second;

      pgsql::value_traits<
          ::std::basic_string< char >,
          pgsql::id_string >::set_value (
        v,
        i.second_value,
        i.second_size,
        i.second_null);
    }
  }

  // email_address
  //

  bool access::composite_value_traits< ::email_address, id_pgsql >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // recipient_
    //
    if (t[0UL])
    {
      i.recipient_value.capacity (i.recipient_size);
      grew = true;
    }

    // domain_
    //
    if (t[1UL])
    {
      i.domain_value.capacity (i.domain_size);
      grew = true;
    }

    return grew;
  }

  void access::composite_value_traits< ::email_address, id_pgsql >::
  bind (pgsql::bind* b,
        image_type& i,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (b);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    std::size_t n (0);
    ODB_POTENTIALLY_UNUSED (n);

    // recipient_
    //
    b[n].type = pgsql::bind::text;
    b[n].buffer = i.recipient_value.data ();
    b[n].capacity = i.recipient_value.capacity ();
    b[n].size = &i.recipient_size;
    b[n].is_null = &i.recipient_null;
    n++;

    // domain_
    //
    b[n].type = pgsql::bind::text;
    b[n].buffer = i.domain_value.data ();
    b[n].capacity = i.domain_value.capacity ();
    b[n].size = &i.domain_size;
    b[n].is_null = &i.domain_null;
    n++;
  }

  bool access::composite_value_traits< ::email_address, id_pgsql >::
  init (image_type& i,
        const value_type& o,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    bool grew (false);

    // recipient_
    //
    {
      ::std::string const& v =
        o.recipient_;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.recipient_value.capacity ());
      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_image (
        i.recipient_value,
        size,
        is_null,
        v);
      i.recipient_null = is_null;
      i.recipient_size = size;
      grew = grew || (cap != i.recipient_value.capacity ());
    }

    // domain_
    //
    {
      ::std::string const& v =
        o.domain_;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.domain_value.capacity ());
      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_image (
        i.domain_value,
        size,
        is_null,
        v);
      i.domain_null = is_null;
      i.domain_size = size;
      grew = grew || (cap != i.domain_value.capacity ());
    }

    return grew;
  }

  void access::composite_value_traits< ::email_address, id_pgsql >::
  init (value_type& o,
        const image_type&  i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // recipient_
    //
    {
      ::std::string& v =
        o.recipient_;

      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_value (
        v,
        i.recipient_value,
        i.recipient_size,
        i.recipient_null);
    }

    // domain_
    //
    {
      ::std::string& v =
        o.domain_;

      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_value (
        v,
        i.domain_value,
        i.domain_size,
        i.domain_null);
    }
  }

  // person
  //

  const char access::object_traits_impl< ::person, id_pgsql >::
  persist_statement_name[] = "persist_person";

  const char access::object_traits_impl< ::person, id_pgsql >::
  find_statement_name[] = "find_person";

  const char access::object_traits_impl< ::person, id_pgsql >::
  update_statement_name[] = "update_person";

  const char access::object_traits_impl< ::person, id_pgsql >::
  erase_statement_name[] = "erase_person";

  const char access::object_traits_impl< ::person, id_pgsql >::
  query_statement_name[] = "query_person";

  const char access::object_traits_impl< ::person, id_pgsql >::
  erase_query_statement_name[] = "erase_query_person";

  const unsigned int access::object_traits_impl< ::person, id_pgsql >::
  persist_statement_types[] =
  {
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::text_oid
  };

  const unsigned int access::object_traits_impl< ::person, id_pgsql >::
  find_statement_types[] =
  {
    pgsql::text_oid,
    pgsql::text_oid
  };

  const unsigned int access::object_traits_impl< ::person, id_pgsql >::
  update_statement_types[] =
  {
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::text_oid
  };

  struct access::object_traits_impl< ::person, id_pgsql >::extra_statement_cache_type
  {
    pgsql::container_statements_impl< name_extras_aliases_traits > name_extras_aliases_;

    extra_statement_cache_type (
      pgsql::connection& c,
      image_type&,
      id_image_type&,
      pgsql::binding& id,
      pgsql::binding&,
      pgsql::native_binding& idn,
      const unsigned int* idt)
    : name_extras_aliases_ (c, id, idn, idt)
    {
    }
  };

  // aliases_
  //

  const char access::object_traits_impl< ::person, id_pgsql >::name_extras_aliases_traits::
  select_name[] = "select_person_name_extras_aliases";

  const char access::object_traits_impl< ::person, id_pgsql >::name_extras_aliases_traits::
  insert_name[] = "insert_person_name_extras_aliases";

  const char access::object_traits_impl< ::person, id_pgsql >::name_extras_aliases_traits::
  delete_name[] = "delete_person_name_extras_aliases";

  const unsigned int access::object_traits_impl< ::person, id_pgsql >::name_extras_aliases_traits::
  insert_types[] =
  {
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::int8_oid,
    pgsql::text_oid,
    pgsql::text_oid
  };

  const char access::object_traits_impl< ::person, id_pgsql >::name_extras_aliases_traits::
  select_statement[] =
  "SELECT "
  "\"c_person_name_extras_aliases\".\"index\", "
  "\"c_person_name_extras_aliases\".\"value_first\", "
  "\"c_person_name_extras_aliases\".\"value_last\" "
  "FROM \"c_person_name_extras_aliases\" "
  "WHERE \"c_person_name_extras_aliases\".\"object_id_recipient\"=$1 AND \"c_person_name_extras_aliases\".\"object_id_domain\"=$2 ORDER BY \"c_person_name_extras_aliases\".\"index\"";

  const char access::object_traits_impl< ::person, id_pgsql >::name_extras_aliases_traits::
  insert_statement[] =
  "INSERT INTO \"c_person_name_extras_aliases\" "
  "(\"object_id_recipient\", "
  "\"object_id_domain\", "
  "\"index\", "
  "\"value_first\", "
  "\"value_last\") "
  "VALUES "
  "($1, $2, $3, $4, $5)";

  const char access::object_traits_impl< ::person, id_pgsql >::name_extras_aliases_traits::
  delete_statement[] =
  "DELETE FROM \"c_person_name_extras_aliases\" "
  "WHERE \"object_id_recipient\"=$1 AND \"object_id_domain\"=$2";

  access::object_traits_impl< ::person, id_pgsql >::id_type
  access::object_traits_impl< ::person, id_pgsql >::
  id (const image_type& i)
  {
    pgsql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      composite_value_traits< ::email_address, id_pgsql >::init (
        id,
        i.email_value,
        db);
    }

    return id;
  }

  bool access::object_traits_impl< ::person, id_pgsql >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // email_
    //
    if (composite_value_traits< ::email_address, id_pgsql >::grow (
          i.email_value, t + 0UL))
      grew = true;

    // name_
    //
    if (composite_value_traits< ::person::name_type, id_pgsql >::grow (
          i.name_value, t + 2UL))
      grew = true;

    // phone_
    //
    if (composite_value_traits< ::phone_numbers, id_pgsql >::grow (
          i.phone_value, t + 6UL))
      grew = true;

    return grew;
  }

  void access::object_traits_impl< ::person, id_pgsql >::
  bind (pgsql::bind* b,
        image_type& i,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    std::size_t n (0);

    // email_
    //
    if (sk != statement_update)
    {
      composite_value_traits< ::email_address, id_pgsql >::bind (
        b + n, i.email_value, sk);
      n += 2UL;
    }

    // name_
    //
    composite_value_traits< ::person::name_type, id_pgsql >::bind (
      b + n, i.name_value, sk);
    n += 4UL;

    // phone_
    //
    composite_value_traits< ::phone_numbers, id_pgsql >::bind (
      b + n, i.phone_value, sk);
    n += 2UL;
  }

  void access::object_traits_impl< ::person, id_pgsql >::
  bind (pgsql::bind* b, id_image_type& i)
  {
    std::size_t n (0);
    pgsql::statement_kind sk (pgsql::statement_select);
    composite_value_traits< ::email_address, id_pgsql >::bind (
      b + n, i.id_value, sk);
  }

  bool access::object_traits_impl< ::person, id_pgsql >::
  init (image_type& i,
        const object_type& o,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    bool grew (false);

    // email_
    //
    if (sk == statement_insert)
    {
      ::email_address const& v =
        o.email_;

      if (composite_value_traits< ::email_address, id_pgsql >::init (
            i.email_value,
            v,
            sk))
        grew = true;
    }

    // name_
    //
    {
      ::person::name_type const& v =
        o.name_;

      if (composite_value_traits< ::person::name_type, id_pgsql >::init (
            i.name_value,
            v,
            sk))
        grew = true;
    }

    // phone_
    //
    {
      ::phone_numbers const& v =
        o.phone_;

      if (composite_value_traits< ::phone_numbers, id_pgsql >::init (
            i.phone_value,
            v,
            sk))
        grew = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::person, id_pgsql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // email_
    //
    {
      ::email_address& v =
        o.email_;

      composite_value_traits< ::email_address, id_pgsql >::init (
        v,
        i.email_value,
        db);
    }

    // name_
    //
    {
      ::person::name_type& v =
        o.name_;

      composite_value_traits< ::person::name_type, id_pgsql >::init (
        v,
        i.name_value,
        db);
    }

    // phone_
    //
    {
      ::phone_numbers& v =
        o.phone_;

      composite_value_traits< ::phone_numbers, id_pgsql >::init (
        v,
        i.phone_value,
        db);
    }
  }

  void access::object_traits_impl< ::person, id_pgsql >::
  init (id_image_type& i, const id_type& id)
  {
    bool grew (false);
    pgsql::statement_kind sk (pgsql::statement_select);
    {
      if (composite_value_traits< ::email_address, id_pgsql >::init (
            i.id_value,
            id,
            sk))
        grew = true;
    }

    if (grew)
      i.version++;
  }

  const char access::object_traits_impl< ::person, id_pgsql >::persist_statement[] =
  "INSERT INTO \"c_person\" "
  "(\"email_recipient\", "
  "\"email_domain\", "
  "\"name_first\", "
  "\"name_last\", "
  "\"name_title\", "
  "\"name_extras_nickname\", "
  "\"phone_first\", "
  "\"phone_second\") "
  "VALUES "
  "($1, $2, $3, $4, $5, $6, $7, $8)";

  const char access::object_traits_impl< ::person, id_pgsql >::find_statement[] =
  "SELECT "
  "\"c_person\".\"email_recipient\", "
  "\"c_person\".\"email_domain\", "
  "\"c_person\".\"name_first\", "
  "\"c_person\".\"name_last\", "
  "\"c_person\".\"name_title\", "
  "\"c_person\".\"name_extras_nickname\", "
  "\"c_person\".\"phone_first\", "
  "\"c_person\".\"phone_second\" "
  "FROM \"c_person\" "
  "WHERE \"c_person\".\"email_recipient\"=$1 AND \"c_person\".\"email_domain\"=$2";

  const char access::object_traits_impl< ::person, id_pgsql >::update_statement[] =
  "UPDATE \"c_person\" "
  "SET "
  "\"name_first\"=$1, "
  "\"name_last\"=$2, "
  "\"name_title\"=$3, "
  "\"name_extras_nickname\"=$4, "
  "\"phone_first\"=$5, "
  "\"phone_second\"=$6 "
  "WHERE \"email_recipient\"=$7 AND \"email_domain\"=$8";

  const char access::object_traits_impl< ::person, id_pgsql >::erase_statement[] =
  "DELETE FROM \"c_person\" "
  "WHERE \"email_recipient\"=$1 AND \"email_domain\"=$2";

  const char access::object_traits_impl< ::person, id_pgsql >::query_statement[] =
  "SELECT "
  "\"c_person\".\"email_recipient\", "
  "\"c_person\".\"email_domain\", "
  "\"c_person\".\"name_first\", "
  "\"c_person\".\"name_last\", "
  "\"c_person\".\"name_title\", "
  "\"c_person\".\"name_extras_nickname\", "
  "\"c_person\".\"phone_first\", "
  "\"c_person\".\"phone_second\" "
  "FROM \"c_person\"";

  const char access::object_traits_impl< ::person, id_pgsql >::erase_query_statement[] =
  "DELETE FROM \"c_person\"";

  const char access::object_traits_impl< ::person, id_pgsql >::table_name[] =
  "\"c_person\"";

  void access::object_traits_impl< ::person, id_pgsql >::
  persist (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              obj,
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    id_image_type& i (sts.id_image ());
    init (i, obj.email_);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    extra_statement_cache_type& esc (sts.extra_statement_cache ());

    // name_.extras_.aliases_
    //
    {
      ::basic_names const& v =
        obj.name_.extras_.aliases_;

      name_extras_aliases_traits::persist (
        v,
        esc.name_extras_aliases_);
    }

    callback (db,
              obj,
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::person, id_pgsql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace pgsql;
    using pgsql::update_statement;

    callback (db, obj, callback_event::pre_update);

    pgsql::transaction& tr (pgsql::transaction::current ());
    pgsql::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj.email_);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    extra_statement_cache_type& esc (sts.extra_statement_cache ());

    // name_.extras_.aliases_
    //
    {
      ::basic_names const& v =
        obj.name_.extras_.aliases_;

      name_extras_aliases_traits::update (
        v,
        esc.name_extras_aliases_);
    }

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::person, id_pgsql >::
  erase (database& db, const id_type& id)
  {
    using namespace pgsql;

    ODB_POTENTIALLY_UNUSED (db);

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    extra_statement_cache_type& esc (sts.extra_statement_cache ());

    // name_.extras_.aliases_
    //
    name_extras_aliases_traits::erase (
      esc.name_extras_aliases_);

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::person, id_pgsql >::pointer_type
  access::object_traits_impl< ::person, id_pgsql >::
  find (database& db, const id_type& id)
  {
    using namespace pgsql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::person, id_pgsql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::person, id_pgsql >::
  reload (database& db, object_type& obj)
  {
    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj.email_);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::person, id_pgsql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace pgsql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  void access::object_traits_impl< ::person, id_pgsql >::
  load_ (statements_type& sts,
         object_type& obj,
         bool reload)
  {
    ODB_POTENTIALLY_UNUSED (reload);

    extra_statement_cache_type& esc (sts.extra_statement_cache ());

    // name_.extras_.aliases_
    //
    {
      ::basic_names& v =
        obj.name_.extras_.aliases_;

      name_extras_aliases_traits::load (
        v,
        esc.name_extras_aliases_);
    }
  }

  result< access::object_traits_impl< ::person, id_pgsql >::object_type >
  access::object_traits_impl< ::person, id_pgsql >::
  query (database&, const query_base_type& q)
  {
    using namespace pgsql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        sts.connection (),
        query_statement_name,
        text,
        false,
        true,
        q.parameter_types (),
        q.parameter_count (),
        q.parameters_binding (),
        imb));

    st->execute ();
    st->deallocate ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) pgsql::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::person, id_pgsql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      erase_query_statement_name,
      text,
      q.parameter_types (),
      q.parameter_count (),
      q.parameters_binding ());

    return st.execute ();
  }
}

#include <odb/post.hxx>
