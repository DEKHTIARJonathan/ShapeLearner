/* This file was generated by ODB, object-relational mapping (ORM)
 * compiler for C++.
 */

DROP TABLE IF EXISTS "boost_pension_fund_members" CASCADE;

DROP TABLE IF EXISTS "boost_pension_fund" CASCADE;

DROP TABLE IF EXISTS "boost_employee_emails" CASCADE;

DROP TABLE IF EXISTS "boost_employee" CASCADE;

DROP TABLE IF EXISTS "boost_employer" CASCADE;

CREATE TABLE "boost_employer" (
  "name" TEXT NOT NULL PRIMARY KEY);

CREATE TABLE "boost_employee" (
  "id" BIGSERIAL NOT NULL PRIMARY KEY,
  "first" TEXT NOT NULL,
  "last" TEXT NOT NULL,
  "employer" TEXT NOT NULL,
  CONSTRAINT "employer_fk"
    FOREIGN KEY ("employer")
    REFERENCES "boost_employer" ("name")
    INITIALLY DEFERRED);

CREATE TABLE "boost_employee_emails" (
  "object_id" BIGINT NOT NULL,
  "value" TEXT NOT NULL,
  CONSTRAINT "object_id_fk"
    FOREIGN KEY ("object_id")
    REFERENCES "boost_employee" ("id")
    ON DELETE CASCADE);

CREATE INDEX "boost_employee_emails_object_id_i"
  ON "boost_employee_emails" ("object_id");

CREATE TABLE "boost_pension_fund" (
  "name" TEXT NOT NULL PRIMARY KEY);

CREATE TABLE "boost_pension_fund_members" (
  "object_id" TEXT NOT NULL,
  "index" BIGINT NOT NULL,
  "value" BIGINT NOT NULL,
  CONSTRAINT "object_id_fk"
    FOREIGN KEY ("object_id")
    REFERENCES "boost_pension_fund" ("name")
    ON DELETE CASCADE,
  CONSTRAINT "value_fk"
    FOREIGN KEY ("value")
    REFERENCES "boost_employee" ("id")
    INITIALLY DEFERRED);

CREATE INDEX "boost_pension_fund_members_object_id_i"
  ON "boost_pension_fund_members" ("object_id");

CREATE INDEX "boost_pension_fund_members_index_i"
  ON "boost_pension_fund_members" ("index");

